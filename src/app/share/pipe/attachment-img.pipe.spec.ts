import { AttachmentImgPipe } from './attachment-img.pipe';

describe('AttachmentImgPipe', () => {
  it('create an instance', () => {
    const pipe = new AttachmentImgPipe();
    expect(pipe).toBeTruthy();
  });
});
